<!-- Відповідає за: вибір адаптера, введення BPF, Start/Stop, та таблицю захоплених подій -->
<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TrafficMonitor" Height="600" Width="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Top">
            <ComboBox Width="420"
                      ItemsSource="{Binding Devices}"
                      SelectedItem="{Binding SelectedDevice}"
                      DisplayMemberPath="Name"/>

            <TextBox Width="300" Margin="8,0,0,0"
                     Text="{Binding BpfFilter}"
                     ToolTip="BPF filter (наприклад: tcp port 443)"/>

            <Button Content="Start" Margin="8,0,0,0" Command="{Binding StartCommand}" />
            <Button Content="Stop" Margin="8,0,0,0" Command="{Binding StopCommand}" />

            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center"
                       Text="{Binding StatusText}"/>
        </StackPanel>

        <!-- Відповідає за таблицю подій захоплення, колонки задаємо вручну -->
        <!-- Відповідає за відображення розпарсених пакетів (PacketInfo) -->
        <DataGrid Grid.Row="2"
          ItemsSource="{Binding Packets}"
          IsReadOnly="True"
          AutoGenerateColumns="False"
          EnableRowVirtualization="True"
          EnableColumnVirtualization="True"
          VirtualizingPanel.IsVirtualizing="True"
          VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss.fff}" Width="220"/>
                <DataGridTextColumn Header="Src" Binding="{Binding SrcIp}" Width="190"/>
                <DataGridTextColumn Header="Dst" Binding="{Binding DstIp}" Width="190"/>
                <DataGridTextColumn Header="Proto" Binding="{Binding Protocol}" Width="90"/>
                <DataGridTextColumn Header="SPort" Binding="{Binding SrcPort}" Width="70"/>
                <DataGridTextColumn Header="DPort" Binding="{Binding DstPort}" Width="70"/>
                <DataGridTextColumn Header="Len" Binding="{Binding Length}" Width="70"/>
                <DataGridTextColumn Header="Flags" Binding="{Binding TcpFlags}" Width="110"/>
                <DataGridTextColumn Header="Info" Binding="{Binding Info}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</Window>
